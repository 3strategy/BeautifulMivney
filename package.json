{
  "private": true,
  "scripts": {
    "build:mdx": "node scripts/build-mdx.mjs",
    "build": "npm run build:mdx && bundle exec jekyll build",
    "serve": "npm run build:mdx && bundle exec jekyll serve"
  },
  "devDependencies": {
    "@mdx-js/mdx": "^3.1.1",
    "esbuild": "^0.25.9",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
