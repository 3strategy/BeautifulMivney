---
layout: page
title: "ex6a1 עצים בינאריים BinNode⟨T⟩"
subtitle: "עצים בינאריים: BinNode⟨T⟩ תרגילים ראשונים"
tags: []
courseName: "מבני נתונים"
unitTopic: "עצים בינאריים"
lang: he
---


## 6a1.1 סכום צמתים בעץ בינארי {#id6a1.1}

עליכם לממש פונקציה בשם `SumBinaryTreeNodes` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה לחשב ולהחזיר את סכום כל הערכים (מספרים שלמים) בצמתי העץ הבינארי.

שימו לב למקרים הבאים:

1. אם העץ ריק (כלומר, tree הוא null), הפונקציה צריכה להחזיר 0.
2. עליכם לעבור על כל הצמתים בעץ ולסכום את ערכיהם.
3. ניתן לפתור את הבעיה באמצעות רקורסיה.

### המחלקה BinNode

מבנה הנתונים `BinNode`: המחלקה `BinNode` ניתנת לכם לשימוש וכוללת את הפעולות הבאות:

1. `()public int GetValue`: מחזירה את ערך הצומת.
2. `()public BinNode GetLeft`: מחזירה את ילד שמאל של הצומת.
3. `()public BinNode GetRight`: מחזירה את ילד ימין של הצומת.
4. `()public bool HasLeft`: מחזירה true אם לצומת יש ילד שמאל.
5. `()public bool HasRight`: מחזירה true אם לצומת יש ילד ימין.

### דוגמאות

1. עבור העץ: 5

    ```mermaid
        flowchart TB
          A[5] --> B[3]
          A --> C[8]
          B --> D[1]:::leaf
          C --> E[4]:::leaf
    ```

   הפונקציה תחזיר: 21 (5 + 3 + 8 + 1 + 4)

1. עבור העץ:

    ```mermaid
    flowchart TB
      A[10] --> B[-2]:::leaf
      A --> C[7]:::leaf
    ```

    הפונקציה תחזיר: 15 (10 + (-2) + 7)

1. עבור עץ ריק (null), הפונקציה תחזיר: 0

## 6a1.2 המספר המקסימלי בעץ בינארי {#id6a1.2}

עליכם לממש פונקציה חיצונית בשם `FindMaxInBinaryTree` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

1. tree - עץ בינארי (מסוג BinNode

הפונקציה צריכה להחזיר את המספר המקסימלי הקיים בעץ.

### דגשים

1. השתמשו בגישה רקורסיבית לפתרון הבעיה.
2. עליכם לטפל במקרה שבו העץ ריק (null). במקרה זה, מומלץ להחזיר ערך שיציין שאין מקסימום (לדוגמה, int.MinValue ב-C#).

### דוגמאות

1. עבור העץ:

    ```mermaid
    flowchart TB
      A[5] --> B[3]
      A --> C[8]:::leaf
      B --> D[1]:::leaf
      B --> E[4]:::leaf
    ```

   הפונקציה תחזיר: 8

2. עבור העץ עם שורש 10 וילדים 2 ו-7:

    ```mermaid
    flowchart TB
      A[10] --> B[2]:::leaf
      A --> C[7]:::leaf
    ```

   הפונקציה תחזיר: 10

3. עבור העץ:

    ```mermaid
    flowchart TB
      A[-1] --> B[-5]:::leaf
      A --> C[-2]:::leaf
    ```

   הפונקציה תחזיר: -1

## 6a1.3 ספירת בנים יחידים {#id6a1.3}

עליכם לממש פונקציה בשם  `CountSingleChildren` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה להחזיר את המספר הכולל של צמתים בעץ שיש להם בדיוק ילד אחד (בן יחיד).

### דגשים

1. יש לממש את הפונקציה באופן רקורסיבי.
2. עץ ריק (null) אינו מכיל בנים יחידים.

### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[5] --> B[2]
      A --> C[8]
      B --> D[1]:::leaf
      C --> E[9]:::leaf
    ```
  
    הפונקציה תחזיר 2 (הבנים היחידים הם 2 ו-8).

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[5]
      A --> C[15]:::leaf
      B --> D[3]:::leaf
    ```

    הפונקציה תחזיר 1 (הבן היחיד הוא 5).

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[1] --> B[2]:::leaf
      A --> C[3]:::leaf
    ```

    הפונקציה תחזיר 0 (אין בנים יחידים).

1. עבור עץ ריק, הפונקציה תחזיר 0.

## 6a1.4 {#id6a1.4} ספירת צמתים זוגיים

עליכם לממש פונקציה בשם `CountEvenNodes` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה להחזיר את כמות הצמתים בעץ המכילים ערכים זוגיים.

שימו לב למקרים הבאים:

1. אם העץ ריק (כלומר, t הוא null), הפונקציה צריכה להחזיר 0.
2. עליכם לעבור על כל הצמתים בעץ ולבדוק את זוגיות ערכם.

### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[2] --> B[4]
      A --> C[5]:::leaf
      B --> D[1]:::leaf
      B --> E[6]:::leaf
    ```

    הפונקציה תחזיר 3 (הצמתים 2, 4, 6 זוגיים).

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[1] --> B[3]:::leaf
      A --> C[5]:::leaf
    ```
    <!-- TEXT-TREE-TO-REMOVE-AFTER-VERIFY 6a1.4-example2 -->
    הפונקציה תחזיר 0 (אין צמתים זוגיים).

1. עבור עץ ריק (null), הפונקציה תחזיר 0.

## 6a1.6 סכום בנים ימניים בעץ בינארי {#id6a1.6}

עליכם לממש פונקציה בשם `SumOfRightChildren` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה לחשב ולהחזיר את סכום כל הערכים של הבנים הימניים בעץ.

### דגשים

1. אם העץ ריק (כלומר, tree הוא null), הפונקציה צריכה להחזיר 0.
2. עליכם לעבור על כל הצמתים בעץ ולבדוק אם קיים להם בן ימני. אם כן, יש להוסיף את ערכו לסכום.
3. אין לשנות את מבנה העץ המקורי במהלך החישוב.

## 6a1.7 ספירת צמתים בעץ בינארי {#id6a1.7}

עליכם לממש פונקציה חיצונית בשם `CountOccurrences` אשר מקבלת שני פרמטרים:

1. עץ בינארי של מספרים שלמים `BinNode⟨int⟩ tree`
2. x - מספר שלם (int) לחיפוש וספירה.

הפונקציה צריכה להחזיר את כמות הצמתים בעץ `tree` שערכם שווה ל-`x`.

### דגשים

1. השתמשו בגישה רקורסיבית לפתרון הבעיה.
2. טפלו במקרה שבו העץ ריק (null).
3. אין לשנות את מבנה העץ המקורי.

### דוגמאות

1. עבור העץ:

    ```mermaid
    flowchart TB
      A[4] --> B[2]
      A --> C[5]:::leaf
      B --> D[1]:::leaf
      B --> E[2]:::leaf
    ```

    והמספר X = 2, הפונקציה תחזיר 2.

1. עבור העץ:

    ```mermaid
    flowchart TB
      A[10] --> B[20]
      A --> C[30]:::leaf
      B --> D[40]:::leaf
      B --> E[50]:::leaf
    ```

    והמספר X = 10, הפונקציה תחזיר 1.

1. עבור העץ:

    ```mermaid
    flowchart TB
      A[7] --> B[7]:::leaf
      A --> C[7]:::leaf
    ```

    והמספר X = 7, הפונקציה תחזיר 3.

## 6a1.8 ספירת עלים בעץ בינארי {#id6a1.8}

עליכם לממש פונקציה סטטית בשם `CountLeaves` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה להחזיר את מספר העלים בעץ הבינארי הנתון.

### דגשים

1. עלה הוא צומת שאין לו ילד שמאלי ואין לו ילד ימני.
2. אם העץ ריק (null), יש להחזיר 0 עלים.
3. אם העץ מכיל רק צומת אחד, צומת זה נחשב לעלה.
4. עליכם להשתמש במחלקה BinNode.
### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[5] --> B[2]
      A --> C[8]:::leaf
      B --> D[1]:::leaf
      B --> E[7]:::leaf
    ```

    העלים הם 1, 7, 8. הפונקציה תחזיר: 3

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[5]
      A --> C[15]
      B --> D[2]:::leaf
      B --> E[7]:::leaf
      C --> F[12]:::leaf
      C --> G[18]:::leaf
    ```

    העלים הם 2, 7, 12, 18. הפונקציה תחזיר: 4

1. עבור עץ המכיל צומת בודד (לדוגמה: 42), הפונקציה תחזיר: 1

    ```mermaid
    flowchart TB
      A[42]:::leaf
    ```

## 6a1.9 צמתים שקטנים מילדם {#id6a1.9}

עליכם לממש פונקציה בשם `CountNodesSmallerThanChild` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה להחזיר את כמות הצמתים בעץ שערכו קטן לפחות מאחד מילדיהם (השמאלי או הימני).

### דגשים

1. יש לממש את הפונקציה באמצעות רקורסיה.
2. צומת נספר אם ערכו קטן מערך הילד השמאלי שלו, או שערכו קטן מערך הילד הימני שלו.
3. אם לצומת אין ילדים, הוא אינו נספר.
4. אם לצומת יש רק ילד אחד (שמאלי או ימני), יש לבדוק רק מולו.
### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[5] --> B[3]
      A --> C[8]:::leaf
      B --> D[6]:::leaf
      B --> E[9]:::leaf
    ```

    הפלט יהיה: 2

    - הצומת 5 קטן מהילד הימני שלו 8, ולכן נספר.
    - הצומת 3 קטן משני ילדיו (6 ו-9), ולכן נספר.
    - הצמתים 6, 9 ו-8 הם עלים ולכן אינם נספרים.

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[4]
      A --> C[15]:::leaf
      B --> D[2]:::leaf
      B --> E[6]:::leaf
    ```

    הפלט יהיה: 2

    - הצומת 10 קטן מהילד הימני שלו 15, ולכן נספר.
    - הצומת 4 קטן מהילד הימני שלו 6, ולכן נספר.

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[1] --> B[2]:::leaf
      A --> C[3]:::leaf
    ```

    הפלט יהיה: 1

    - הצומת 1 קטן משני ילדיו (2 ו-3), ולכן נספר.

1. עבור עץ עם צומת בודד (לדוגמה, 7):

    ```mermaid
    flowchart TB
      A[7]:::leaf
    ```

    הפלט יהיה: 0 (אין לו ילדים).

1. עבור עץ ריק:הפלט יהיה: 0

## 6a1.10  ספירת צמתים גדולים מהאב בעץ בינארי {#id6a1.10}

עליכם לממש פונקציה בשם `CountNodesGreaterThanParent` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה להחזיר את מספר הצמתים בעץ שערכם גדול מערך האב שלהם.

### דגשים

1. אין צורך להתייחס לשורש העץ, מכיוון שלשורש אין אב.
2. יש להשתמש במחלקת BinNode עבור צמתי העץ.
3. הפתרון צריך להיות רקורסיבי.
4. ודאו שאתם מטפלים כראוי במקרה של עץ ריק או עץ עם צומת שורש בלבד.

### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[5]
      A --> C[15]
      B --> D[3]:::leaf
      B --> E[4]:::leaf
      C --> F[12]:::leaf
      C --> G[18]:::leaf
    ```

    - הצומת 15 (אב 10) גדול מאביו. (ספירה: 1)
    - הצומת 18 (אב 15) גדול מאביו. (ספירה: 2)הפונקציה תחזיר: 2

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[20] --> B[10]
      A --> C[30]
      B --> D[5]:::leaf
      B --> E[15]:::leaf
      C --> F[25]:::leaf
      C --> G[35]:::leaf
    ```

    הפונקציה תחזיר 3.

## 6a1.11 צמתים אי-זוגיים ללא אח {#id6a1.11}

עליכם לממש פונקציה חיצונית בשם `SumOddSingleNodes` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה לחזור את סכום כל הצמתים היחידים (צמתים שאין להם אח (למעט השורש) שערכם אי זוגי.

### דגשים

1. יש להשתמש במחלקה BinNode.
2. שימו לב שערך הצומת חייב להיות אי-זוגי כדי להיכלל בסכום.

### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[5]
      A --> C[20]
      B --> D[3]:::leaf
      C --> E[15]:::leaf
    ```

    הצמתים היחידים הם 3 (בן יחיד של 5) ו-15 (בן יחיד של 20). שניהם אי-זוגיים. הפלט יהיה: 18 (3 + 15)

2. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[1] --> B[2]
      A --> C[3]
      B --> D[4]:::leaf
      C --> E[5]:::leaf
    ```

    הצמתים היחידים הם 4 (בן יחיד של 2) ו-5 (בן יחיד של 3). שניהם אי-זוגיים. הפלט יהיה: 9 (4 + 5)

3. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[2]:::leaf
      A --> C[4]:::leaf
    ```

    אין צמתים יחידים. הפלט יהיה: 0

## 6a1.12 כמות סבים בעץ בינארי {#id6a1.12}

עליכם לממש פונקציה חיצונית בשם `CountGrandparents` אשר מקבלת `BinNode⟨int⟩ tree` המצביע לשורש של עץ בינארי

הפונקציה צריכה לחשב ולהחזיר את כמות הסבים בעץ.

### הגדרת סב

1. צומת נחשב ל'סב' אם יש לו לפחות נכד אחד. כלומר, אם יש לו ילד (שמאלי או ימני) שלילד זה יש ילד משלו (שמאלי או ימני).

### דגשים

1. אין לשנות את מבנה העץ המקורי.
2. השתמשו במחלקת BinNode.
3. שימו לב למקרים בהם צמתים מסוימים או כל העץ ריקים.

### דוגמאות

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[10] --> B[5]
      A --> C[15]
      B --> D[2]:::leaf
      B --> E[7]:::leaf
      C --> F[12]:::leaf
      C --> G[17]:::leaf
    ```

    הצומת 10 הוא סב (יש לו נכדים: 2, 7, 12, 17). הפונקציה תחזיר 1.

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[20] --> B[10]:::leaf
      A --> C[30]:::leaf
    ```

    אין סבים בעץ זה. הפונקציה תחזיר 0.

1. עבור העץ הבא:

    ```mermaid
    flowchart TB
      A[1] --> B[2]
      A --> C[3]
      B --> D[4]:::leaf
      C --> E[5]:::leaf
    ```

    הצומת 1 הוא סב (יש לו נכדים: 4, 5). הפונקציה תחזיר 1.
